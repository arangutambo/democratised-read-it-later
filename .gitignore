node_modules/

# Build artifacts — produced by esbuild, not committed.
main.js
styles.css
*.js.map

# Dry-run bundles — esbuild scratch output from the tools/ scripts.
.*.cjs

# Local plugin data, never shared.
data.json

# The real Apple Books corpus used by the anchoring benchmark: private, copyrighted,
# and never committed. See PLAN.md §5.
test/private/

# Planning and design notes. They quote a private vault — its size, its
# broken references, its Zotero library, the coursework in it — and none of
# that belongs in a public repo. Kept locally.
READER-V2-PROMPT.md
INTERROGATION-PROMPT.md
PLAN.md
PLAN-V2.md
DESIGN.md
SOURCES.md

.DS_Store
